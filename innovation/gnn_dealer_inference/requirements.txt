# Core deep learning (CPU-only)
# Install torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# PyTorch Geometric (primary backend)
# CPU-only install:
#   pip install torch_geometric
#   pip install pyg_lib torch_scatter torch_sparse \
#       -f https://data.pyg.org/whl/torch-$(python -c "import torch; print(torch.__version__)")+cpu.html
torch_geometric>=2.5.0

# Optional: DGL fallback if PyG install fails
# pip install dgl -f https://data.dgl.ai/wheels/torch-2.5/repo.html
# dgl>=2.0.0

# Serving
flask>=3.0.0

# Training utilities
scikit-learn>=1.3.0

# Data
numpy>=1.26.0
