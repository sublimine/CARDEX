# GNN dealer inference — CPU-only dependencies
#
# Install:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch_geometric flask scikit-learn numpy
#
# Optional (may fail on ARM64 / Alpine — DGL fallback available):
#   pip install pyg_lib torch_scatter torch_sparse \
#     -f https://data.pyg.org/whl/torch-<version>+cpu.html
#
# DGL fallback:
#   pip install dgl -f https://data.dgl.ai/wheels/repo.html

torch>=2.0.0
torch_geometric>=2.4.0
flask>=3.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
pytest>=7.0.0
