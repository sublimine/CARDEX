**/node_modules/
**/.next/
bin/
*.exe
.env
.env.*
*.log
.vscode/
.cursor/
.claude/
!.env.example

# JWT keys (generated locally by 'make gen-keys')
secrets/
*.pem

# Python
**/__pycache__/
*.pyc

# Local tooling / debug artifacts (Playwright MCP cache, dev scratch, debug screenshots)
.playwright-mcp/
dev-log.txt
identity-section.png
sidebar-dark.png

# SQLite scraper runtime state (work_queue engine DB + WAL/SHM sidecars)
scrapers/engine.db
*.db-shm
*.db-wal

# pytest cache
pytest-cache-files-*/
.pytest_cache/

# Run artifacts at repo root (harness outputs, NOT the dealer recipes under configs/)
/inv_*.json
/close_*.json
/*_results.json
/proof_*.json
/dealer_scraping_results.json
logs/

# Machine-specific operational scratch (hardcoded local paths, OOM workarounds,
# monitor loops that emit the already-ignored *.log files)
.audit_run.ps1
auto_commit_check.ps1
fix_oom.ps1
scrapers/_monitor_loop.ps1
scrapers/_qstat.py
scrapers/monitor.sh
